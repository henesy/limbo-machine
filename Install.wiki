#summary How to install and run the system.
#labels Phase-Deploy

  * Build and run the server program:
  ; mk clean build start
  * Then open a browser (firefox is the only supported browser as for now) on your native OS and go to:
  file://<limbo.machine.home>/js/sample.html